class Logic {
    // this class really shouldn't edit the values of any given parameter.
    // just let it do calculations. After all, it is only concerned about
    // the game logic.

    removePieces(selectedPiece, squares) {
        // return the coordinates of pieces to remove.
        // Let the board actually remove the pieces from the view.
    }

    checkIfShouldKing(selectedPiece) {
        // return true or false.
        // let the board update the selected piece.
    }

    getPossibleMoves(selectedPiece, squares) {
        // let the board worry about the highlighting.
        // Just return the coordinates of each possible move.
    }

    
}